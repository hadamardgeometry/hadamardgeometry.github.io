Note: Some page PNGs are missing.  Cause not yet looked into.

PNGs are generated by the OCR engine.  It might be simpler (and more reliable) to take them directly 
from the PDF (e.g. using `pdftk`).
